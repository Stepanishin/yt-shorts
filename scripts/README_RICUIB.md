# 📂 RicUIB Scripts - Скрипты для импорта испанских шуток

## 🚀 Основные скрипты

### 1. `import-ricuib-jokes.ts` ⭐ ГЛАВНЫЙ
**Что делает**: Импортирует 500 случайных шуток в базу данных

**Использование**:
```bash
npx tsx scripts/import-ricuib-jokes.ts
```

**Критерии отбора**:
- ✅ `added: false`
- ✅ Длина: 30-700 символов

**Результат**:
- Добавляет в MongoDB
- Помечает `added: true` в JSON

---

### 2. `check-db-jokes.ts`
**Что делает**: Показывает статистику шуток в базе данных

**Использование**:
```bash
npx tsx scripts/check-db-jokes.ts
```

**Покажет**:
- Всего шуток в БД
- Сколько из RicUIB
- Распределение по источникам
- Статусы (pending, used, deleted)
- Примеры шуток

---

### 3. `sync-ricuib-added-status.ts`
**Что делает**: Синхронизирует поле `added` в JSON с базой данных

**Использование**:
```bash
npx tsx scripts/sync-ricuib-added-status.ts
```

**Когда нужно**:
- После ручного удаления шуток из БД
- Если несоответствие между файлом и БД
- Для проверки консистентности

---

### 4. `delete-old-ricuib-jokes.ts`
**Что делает**: Удаляет шутки со старым форматом externalId из БД

**Использование**:
```bash
npx tsx scripts/delete-old-ricuib-jokes.ts
```

⚠️ **ОСТОРОЖНО**: Удалит все шутки без нового формата!

**Когда нужно**:
- При миграции на новый формат
- Для очистки БД

---

### 5. `reset_added_flags.py`
**Что делает**: Сбрасывает все `added: true` → `added: false` в JSON файле

**Использование**:
```bash
python3 scripts/reset_added_flags.py
```

**Когда нужно**:
- Начать импорт с нуля
- После удаления всех шуток из БД

---

### 6. `verify-new-format.ts`
**Что делает**: Проверяет формат externalId в базе данных

**Использование**:
```bash
npx tsx scripts/verify-new-format.ts
```

**Покажет**:
- Примеры externalId
- Сколько с новым форматом
- Сколько со старым форматом

---

## 📖 Примеры использования

### Добавить 500 шуток (первый раз)
```bash
npx tsx scripts/import-ricuib-jokes.ts
```

### Добавить еще 500 шуток
```bash
# Проверить состояние
npx tsx scripts/check-db-jokes.ts

# Добавить еще 500
npx tsx scripts/import-ricuib-jokes.ts
```

### Начать с нуля
```bash
# 1. Удалить из БД
npx tsx scripts/delete-old-ricuib-jokes.ts

# 2. Сбросить файл
python3 scripts/reset_added_flags.py

# 3. Добавить 500 шуток
npx tsx scripts/import-ricuib-jokes.ts
```

### Проверить консистентность
```bash
# Показать статистику БД
npx tsx scripts/check-db-jokes.ts

# Синхронизировать файл с БД
npx tsx scripts/sync-ricuib-added-status.ts
```

---

## 🔧 Технические детали

### Формат externalId

✅ **Новый** (используется):
```
ricuib:1000 chistes:123
ricuib:Pintamania:456
```

❌ **Старый** (не используется):
```
1000_chistes:Titulo
Pintamania:JAIMITO
```

### Где хранятся данные

**MongoDB коллекция**: `joke_candidates`

**JSON файл**: `chistes_ricuib.json` (7,169 шуток)

### Источники данных

- **ricuib-1000chistes** - из сайта 1000chistes.com
- **ricuib-pintamania** - из сайта Pintamania

---

## 📚 Полная документация

Смотрите: `RICUIB_IMPORT_INSTRUCTIONS.md` в корне проекта

---

**Быстрая справка**: `RICUIB_QUICK_START.md`
