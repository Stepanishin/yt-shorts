# YouTube Shorts Generator - Подробный план проекта

## 📋 Общее описание

Веб-приложение для автоматической генерации YouTube Shorts с испанскими анекдотами. Приложение предназначено для одного пользователя и включает полный цикл: от парсинга анекдотов до загрузки готового видео на YouTube.

## 🎯 Основные цели

1. Автоматизация процесса создания контента для YouTube Shorts
2. Генерация уникальных видео с испанскими анекдотами
3. Минимизация ручной работы при создании контента
4. Отслеживание уже использованных анекдотов для избежания дубликатов

## 🛠 Технологический стек

### Frontend
- **Next.js 16** (App Router) - основной фреймворк
- **TypeScript** - типизация для надежности кода
- **Tailwind CSS** - стилизация интерфейса
- **React 19** - UI компоненты

### Backend
- **Next.js API Routes** - серверные эндпоинты
- **MongoDB** - база данных для хранения анекдотов и истории генераций
- **Mongoose** - ODM для работы с MongoDB

### Внешние API и сервисы
- **OpenAI API** - генерация контента, обработка текста
- **Google YouTube Data API v3** - загрузка видео на YouTube
- **Web Scraping библиотеки** (cheerio/puppeteer) - парсинг анекдотов с сайтов (если нет API)
- **Музыкальные сервисы** - генерация/получение фоновой музыки:
  - Suno AI API (генерация музыки)
  - или Epidemic Sound API (библиотека роялти-фри музыки)
  - или YouTube Audio Library (бесплатная музыка)
  - или Mubert API (AI генерация музыки)

### Генерация видео
- **Remotion** - программная генерация видео с помощью React
- **Remotion Lambda** - рендеринг видео в AWS (обход ограничений Vercel)
- **FFmpeg** (в Lambda) - обработка аудио и финальная сборка

## 📐 Архитектура приложения

### Vercel-специфичная архитектура

#### Ограничения Vercel и решения:

**Проблема 1: Timeout функций**
- Hobby: 10 секунд
- Pro: 60 секунд  
- **Решение**: Использовать Remotion Lambda для рендеринга (без ограничений)

**Проблема 2: Хранение файлов**
- Vercel не для хранения больших файлов
- **Решение**: Cloudflare R2 для всех видео/музыки

**Проблема 3: Фоновые задачи**
- Нет long-running процессов
- **Решение**: 
  - Асинхронные задачи через Remotion Lambda
  - Статусы задач в MongoDB
  - Polling с фронтенда каждые 3-5 секунд

#### Архитектура генерации видео:

```
┌─────────────┐
│   Vercel    │
│  Next.js    │
│   Frontend  │
└──────┬──────┘
       │ 1. Запрос генерации
       ↓
┌──────────────────┐
│  Vercel API      │
│  /api/videos/    │
│  generate        │
└────┬─────────────┘
     │ 2. Создание записи в MongoDB (status: 'pending')
     │ 3. Вызов Remotion Lambda
     ↓
┌─────────────────────┐
│  Remotion Lambda    │
│  (AWS)              │
│  - Генерация фона   │
│  - Наложение текста │
│  - Анимация эмодзи  │
│  - Добавление музыки│
│  - Рендеринг видео  │
└────┬────────────────┘
     │ 4. Сохранение в Cloudflare R2
     │ 5. Обновление MongoDB (status: 'completed')
     ↓
┌──────────────────┐
│  Cloudflare R2   │
│  (видео/музыка)  │
└──────────────────┘
     ↑
     │ 6. Polling статуса
┌────┴─────────┐
│   Frontend   │
│  (каждые 3с) │
└──────────────┘
```

#### Workflow генерации:

1. **Пользователь** нажимает "Сгенерировать видео"
2. **Frontend** вызывает `/api/videos/generate`
3. **API** создает запись в MongoDB со статусом `pending`
4. **API** запускает Remotion Lambda через `@remotion/lambda`
5. **Frontend** начинает polling `/api/videos/[id]` каждые 3 секунды
6. **Lambda** генерирует видео (~1-2 минуты)
7. **Lambda** загружает в R2 и обновляет MongoDB на `completed`
8. **Frontend** получает статус и показывает готовое видео

### Структура базы данных (MongoDB)

#### Collection: `jokes`
```javascript
{
  _id: ObjectId,
  text: String,                 // Очищенный текст анекдота (для генерации)
  rawHtml: String,              // Исходный HTML (для перепроверки/повторной очистки)
  language: String,             // Язык (например, "es")

  sourceSite: String,           // "yavendras" | "todochistes" | "chistes" | "jokeapi"
  sourceCategory: String,       // Категория на сайте (например, "Pepito", "Abogados")
  sourceUrl: String,            // Прямая ссылка на страницу шутки
  sourceListUrl: String,        // URL категории/страницы где найдено
  externalId: String,           // ID на сайте (например, Chistes_Positivo_3288)

  ratingScore: Number,          // Процент положительных голосов (если есть)
  ratingUp: Number,             // Кол-во лайков
  ratingDown: Number,           // Кол-во дизлайков

  lengthTokens: Number,         // Кол-во токенов (для оценки длины)
  tags: [String],               // Доп. метки ("family_friendly", "needs_review", ...)
  safeForYoutube: Boolean,      // Пройдена ли модерация (AI + правила)

  used: Boolean,                // Использован ли анекдот
  usedAt: Date,                 // Когда использован
  createdAt: Date,              // Когда добавлен в базу
  updatedAt: Date,              // Когда обновлялся метаданные
  hash: String,                 // Контроль уникальности (hash от текста)

  moderation:
    {
      openaiResult: Object,     // Результат модерации OpenAI (raw JSON)
      manualReview: Boolean,    // Требуется ли ручная проверка
      reason: String            // Комментарий модератора/причина отказа
    }
}
```

#### Collection: `videos`
```javascript
{
  _id: ObjectId,
  jokeId: ObjectId,          // Ссылка на анекдот
  videoPath: String,         // Путь к сгенерированному видео
  thumbnail: String,         // Путь к превью
  duration: Number,          // Длительность в секундах
  background: String,        // Тип/ID использованного фона
  backgroundImage: String,   // URL/путь к изображению фона
  emoji: String,             // Использованный эмодзи
  musicTrack: String,        // Путь к использованному треку
  musicId: String,           // ID трека (если из библиотеки)
  status: String,            // 'generating', 'generated', 'uploaded', 'failed'
  youtubeId: String,         // ID видео на YouTube (после загрузки)
  youtubeUrl: String,        // Ссылка на YouTube
  createdAt: Date,
  uploadedAt: Date,
  renderTime: Number         // Время рендеринга в секундах
}
```

#### Collection: `sources`
```javascript
{
  _id: ObjectId,
  url: String,               // URL источника анекдотов
  name: String,              // Название источника
  domain: String,            // Домен (yavendras.com, todochistes.net, ...)
  type: String,              // 'scraper' | 'api' | 'hybrid'
  categories: [String],      // Доступные / whitelisted категории
  selector: Object,          // CSS селекторы (listSelector, itemSelector, textSelector, ...)
  pagination: Object,        // Настройки пагинации (pageParam, maxPages, delay)
  apiConfig: Object,         // Конфигурация API (endpoint, headers, params)
  requiresProxy: Boolean,    // Нужен ли прокси/VPN
  throttle: Object,          // Ограничение скорости (requestsPerMinute, delayMs)
  active: Boolean,           // Активен ли источник
  lastScraped: Date,         // Последний парсинг
  scrapedCount: Number,      // Сколько анекдотов спарсено
  errorCount: Number,        // Сколько ошибок подряд
  notes: String              // Комментарии (ограничения, предупреждения)
}
```

#### Collection: `music`
```javascript
{
  _id: ObjectId,
  title: String,             // Название трека
  filePath: String,          // Путь к файлу
  duration: Number,          // Длительность в секундах
  source: String,            // Источник (suno, epidemic, youtube_library)
  genre: String,             // Жанр (upbeat, chill, funny)
  usedCount: Number,         // Сколько раз использован
  lastUsed: Date,            // Когда последний раз использован
  createdAt: Date
}
```

### Источники анекдотов и стратегия сбора

#### 1. Yavendras (`chistes.yavendras.com`)
- **Тип**: HTML-скрейпинг (статические `.php` и `.htm` страницы)
- **Покрытие**: тысячи шуток, десятки категорий, рейтинги пользователей
- **План**:
  - Сформировать белый список категорий (семейные, школьные, Pepito, animales и т.д.)
  - Реализовать пагинацию с задержками (2–3 сек между запросами)
  - На странице шутки извлекать текст, ID (`Chistes_Positivo_XXXX`), рейтинг, URL, автора
  - Сохранять сырое HTML (`rawHtml`) и очищенный текст, помечать `sourceSite="yavendras"`
  - Исключать категории `Humor negro`, `Picantes`, `Machistas` и пр. до модерации

#### 2. TodoChistes (`todochistes.net`)
- **Тип**: WordPress (HTML + REST API)
- **План**:
  - Использовать WP REST (`/wp-json/wp/v2/posts`) для получения полного текста и метаданных
  - Фильтровать категории по whitelisted списку (Abogados, Animales, Doctores, etc.)
  - Для постов с `Read More` запрашивать полный `content.rendered`
  - Извлекать дату публикации, slug, featured image (для превью)
  - Добавлять поля `sourceSite="todochistes"`, `externalId=post.id`, `rating` (если есть)

#### 3. Chistes.com (`api.chistes.com/random`)
- **Тип**: предполагаемый JSON API (требует доступа из разрешённого региона)
- **Статус**: прямые запросы блокируются (Gateway GEO-IP Filter) при обращении из Европы
- **План**:
  - Использовать облачный прокси/сервер в разрешённой стране или VPN
  - Если API стабилен — подключить как дополнительный источник случайных шуток
  - Ввести квоты (не более 30 req/час) и автоматический fallback на другие источники

#### 4. JokeAPI (`https://v2.jokeapi.dev`)
- **Тип**: публичный REST, `lang=es`, поддерживает фильтрацию NSFW
- **Роль**: резервный источник (когда сайты недоступны)
- **План**:
  - Использовать `type=single`, `blacklistFlags=nsfw,religious,political,racist,sexist,explicit`
  - Проверять поле `safe` и длину текста (>= 20 и <= 120 слов)
  - Сохранять `sourceSite="jokeapi"`, `externalId=id`

#### Общий workflow сбора
1. Планировщик (manual/cron) запускает `ingest` сервис.
2. Для каждого источника → очередь `scrape_jobs` в MongoDB:
   - `status: pending | running | failed | completed`
   - параметры: категория, страница, язык, требуется ли прокси
3. Worker обрабатывает job:
   - скачивает страницу/JSON, парсит текст
   - вычисляет `hash` и проверяет дубликаты (`jokes.hash`)
   - прогоняет через модуль цензуры (фильтр слов + OpenAI moderation)
   - сохраняет в `jokes` с `safeForYoutube=true/false`
4. После ingest → обновляется статистика для Dashboard (кол-во новых шуток, ошибки)

#### Реализация Dev-инструментов (MVP)
- `collectJokePreview()` объединяет Chistes, Yavendras, TodoChistes в единый формат (`JokeCandidate`)
- `POST /api/ingest/preview` — сохраняет выбранные превью-шутки в коллекцию `joke_candidates`
- `GET /api/ingest/queue` — просмотр последних сохраненных шуток (по умолчанию 50, максимум 200)
- `POST /api/ingest/queue/reserve` — выдаёт первую `pending` шутку и помечает её `reserved`
- `POST /api/ingest/queue/status` — обновляет статус шутки после генерации/фильтрации
- `POST /api/ingest/run` — собирает шутки по дефолтной конфигурации (Chistes random + топовые категории Yavendras + TodoChistes)
- Конфигурация источников хранится в `lib/ingest/config.ts` (можно расширять списки категорий, таймауты, базовые URL)
- `POST /api/videos/generate` — создаёт задачу генерации (резервирует шутку, создаёт запись в `video_jobs`)
- `GET /api/videos/jobs` — просмотр состояния задач генерации

#### Фильтрация и модерация
- Базовый список стоп-слов (грубая лексика, расизм, NSFW)
- Использование OpenAI Moderation API (бюджетно: GPT-4o-mini moderation endpoint)
- Если модерация не уверена или категория сомнительная → `manualReview=true`
- Видео генерируются только из `safeForYoutube=true` и `manualReview=false`

#### Rate limits & устойчивость
- Yavendras: max 20 запросов/мин, задержка 2 сек, ретраи 3 раза
- TodoChistes: max 30 запросов/мин, использовать `If-Modified-Since`
- Chistes.com: max 10 запросов/мин, нужен прокси/регион
- JokeAPI: уважать guideline (<60 запросов/мин), резервный канал
- Логирование ошибок по каждому источнику (`sources.errorCount`)
- Автоматическое отключение источника `active=false`, если >5 ошибок подряд

## 🔐 Аутентификация и безопасность

### Система авторизации
- Простая авторизация по паролю для одного пользователя
- Пароль хранится в `.env` файле в хешированном виде (bcrypt)
- JWT токен для поддержания сессии
- Middleware для защиты всех API роутов
- Session хранится в httpOnly cookies

### Переменные окружения (.env)
```
# Аутентификация
AUTH_PASSWORD_HASH=<bcrypt хеш пароля>
JWT_SECRET=<секретный ключ для JWT>

# MongoDB
MONGODB_URI=<строка подключения к MongoDB>

# OpenAI
OPENAI_API_KEY=<ключ API>

# YouTube
YOUTUBE_CLIENT_ID=<OAuth2 Client ID>
YOUTUBE_CLIENT_SECRET=<OAuth2 Client Secret>
YOUTUBE_REFRESH_TOKEN=<токен для автоматической загрузки>

# Музыка (выбрать один вариант)
MUSIC_SERVICE=suno # или epidemic, youtube_library, mubert
SUNO_API_KEY=<если используется Suno>
EPIDEMIC_API_KEY=<если используется Epidemic Sound>
MUBERT_API_KEY=<если используется Mubert>

# Хранилище
CLOUDFLARE_R2_ACCOUNT_ID=<Account ID>
CLOUDFLARE_R2_ACCESS_KEY_ID=<Access Key>
CLOUDFLARE_R2_SECRET_ACCESS_KEY=<Secret Key>
CLOUDFLARE_R2_BUCKET_NAME=shorts-videos

# Remotion Lambda (для рендеринга видео)
REMOTION_AWS_ACCESS_KEY_ID=<AWS Access Key>
REMOTION_AWS_SECRET_ACCESS_KEY=<AWS Secret>
REMOTION_AWS_REGION=us-east-1

# Приложение
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app
```

## 📱 Структура интерфейса (все на русском языке)

### 1. Страница авторизации (`/login`)
- Простая форма с полем для пароля
- Кнопка "Войти"
- Валидация на клиенте и сервере

### 2. Главная панель (`/dashboard`)
После авторизации пользователь видит:

#### Верхняя панель
- Статистика:
  - Всего анекдотов в базе
  - Неиспользованных анекдотов
  - Сгенерировано видео
  - Загружено на YouTube

#### Секция "Источники анекдотов"
- Список сохраненных источников
- Кнопка "Добавить источник"
- Кнопка "Спарсить анекдоты" для каждого источника
- Статус последнего парсинга

#### Секция "Генерация видео"
- Кнопка "Сгенерировать новое видео"
- Настройки генерации:
  - Выбор типа фона (природа: лес, море, горы, закат и т.д.)
  - Выбор стиля текста
  - Длительность показа текста
- Прогресс генерации (live обновления)

#### Секция "Библиотека видео"
- Таблица/сетка всех сгенерированных видео:
  - Превью видео
  - Текст анекдота (первые 50 символов)
  - Статус (сгенерировано/загружено/ошибка)
  - Дата создания
  - Действия:
    - Просмотр
    - Скачать
    - Загрузить на YouTube
    - Удалить

### 3. Страница просмотра видео (`/video/[id]`)
- Плеер для просмотра
- Полный текст анекдота
- Информация о видео
- Кнопка загрузки на YouTube
- Кнопка скачивания

## 🎬 Процесс генерации видео

### Этап 1: Получение анекдота
1. Выбор неиспользованного анекдота из базы данных
2. Проверка длины анекдота (оптимально 30-60 секунд чтения)
3. Отметка анекдота как "используемый"

### Этап 2: Подготовка контента

#### Обработка текста с помощью OpenAI
- Проверка грамматики анекдота
- Разбиение на части для покадрового показа
- Генерация тегов для YouTube
- Генерация описания для YouTube

#### Генерация фона
**Варианты реализации:**

**Вариант А: Статические изображения с анимацией**
1. Использовать OpenAI DALL-E для генерации изображения природы
2. Применить Ken Burns эффект (zoom in/out + pan)
3. Добавить цветовые фильтры для живости

**Вариант Б: Видео фоны**
1. Использовать бесплатные стоковые видео (Pexels API, Pixabay)
2. Скачать короткое видео природы
3. Применить цветокоррекцию

#### Подбор музыки
**Варианты реализации:**

**Вариант А: AI генерация (Suno/Mubert)**
1. Генерация уникального трека через API
2. Жанр: upbeat, comedy, light
3. Длительность: соответствует длине видео
4. Кеширование сгенерированных треков

**Вариант Б: Библиотека готовых треков**
1. Предзагрузка коллекции роялти-фри треков
2. Случайный выбор из библиотеки
3. Отслеживание использования (чтобы не повторялись часто)

**Вариант В: YouTube Audio Library**
1. Использование бесплатной библиотеки YouTube
2. Ручная загрузка треков в проект
3. Случайный выбор при генерации

**Рекомендация:** Вариант В (YouTube Audio Library) для начала, затем можно добавить AI генерацию

#### Обработка аудио
- Нормализация громкости (-18 LUFS)
- Fade in/out (0.5 секунды)
- Обрезка под длину видео
- Микширование с FFmpeg

### Этап 3: Композиция видео

#### Параметры видео
- Формат: 9:16 (вертикальный для Shorts)
- Разрешение: 1080x1920 (Full HD) или 720x1280 (HD)
- FPS: 30
- Длительность: 15-60 секунд (оптимально 30-45)
- Кодек: H.264
- Аудио: AAC, 128kbps

#### Слои (снизу вверх):
1. **Аудио слой** - фоновая музыка
2. **Фоновый слой** - анимированное изображение/видео природы
3. **Оверлей** - полупрозрачное затемнение (для читаемости текста)
4. **Текстовый слой** - анекдот с анимацией появления
5. **Эмодзи слой** - анимированный смеющийся эмодзи

#### Анимация текста
- Появление: fade in + slide up
- Показ: статичный или легкое покачивание
- Уход: fade out (при переходе к следующей части)

#### Анимация эмодзи
- Выбор случайного смеющегося эмодзи: 😂, 🤣, 😆, 😹
- Позиция: справа от текста
- Анимация: 
  - Масштабирование (pulse эффект)
  - Небольшое вращение
  - Bounce эффект
- Частота анимации: каждые 0.5-1 секунда

#### Технология генерации видео

**Рекомендуемый стек:**
```
Remotion + FFmpeg
```

**Почему Remotion:**
- Программная генерация видео на React
- Полный контроль над анимациями
- TypeScript поддержка
- Встроенный рендеринг через FFmpeg
- Возможность предпросмотра

**Альтернатива (чистый FFmpeg):**
- Генерация кадров с Canvas
- Использование FFmpeg для сборки
- Более низкоуровневый контроль

### Этап 4: Рендеринг
1. Подбор музыкального трека
2. Запуск процесса рендеринга
3. Наложение аудио дорожки
4. Показ прогресса пользователю (WebSocket/SSE)
5. Сохранение готового видео в `/public/videos/` (или облако)
6. Генерация thumbnail
7. Сохранение записи в БД с метаданными

### Этап 5: Загрузка на YouTube (опционально)

#### YouTube API интеграция
1. OAuth2 аутентификация (одноразовая настройка)
2. Сохранение refresh token в .env
3. Автоматическая загрузка через API

#### Метаданные видео:
- **Название**: Генерируется OpenAI (краткое, привлекающее внимание)
- **Описание**: Короткое описание + хештеги
- **Теги**: #shorts, #chistes, #humor, #español
- **Категория**: Comedy
- **Язык**: Испанский (es)
- **Приватность**: По выбору пользователя

## 🔄 Workflow пользователя

### Первичная настройка (один раз)
1. Авторизация в приложении
2. Добавление источников анекдотов
3. Первый парсинг анекдотов (заполнение базы)
4. (Опционально) Настройка YouTube API

### Ежедневная работа
1. Открыть приложение
2. Нажать "Сгенерировать новое видео"
3. Дождаться генерации (1-3 минуты)
4. Просмотреть результат
5. Загрузить на YouTube или скачать

### Периодическая работа
- Парсинг новых анекдотов (по мере расхода)
- Добавление новых источников
- Просмотр статистики

## 🔧 Основные компоненты и модули

### Frontend компоненты

1. **Auth**
   - `LoginForm.tsx` - форма авторизации
   - `AuthGuard.tsx` - HOC для защиты страниц

2. **Dashboard**
   - `StatsCard.tsx` - карточка статистики
   - `SourcesList.tsx` - список источников
   - `VideoGenerator.tsx` - форма генерации
   - `VideoLibrary.tsx` - библиотека видео
   - `ProgressBar.tsx` - прогресс генерации

3. **Video**
   - `VideoPlayer.tsx` - плеер
   - `VideoCard.tsx` - карточка видео
   - `UploadButton.tsx` - кнопка загрузки на YT

### Backend модули

1. **Auth**
   - `auth.service.ts` - логика авторизации
   - `jwt.utils.ts` - работа с JWT
   - `middleware/auth.ts` - защита роутов

2. **Database**
   - `db.ts` - подключение к MongoDB
   - `models/Joke.ts` - модель анекдота
   - `models/Video.ts` - модель видео
   - `models/Source.ts` - модель источника

3. **Scraping**
   - `ingest.controller.ts` - REST/cron endpoint для запуска сборки
   - `job-queue.ts` - очередь заданий (MongoDB коллекция + in-process worker)
   - `scraper.service.ts` - базовый движок (HTTP клиент, очереди, ретраи)
   - `adapters/yavendras.ts` - правила парсинга для Yavendras
   - `adapters/todochistes.ts` - интеграция с WordPress API + HTML fallback
   - `adapters/chistes.ts` - доступ к `api.chistes.com` (поддержка прокси)
   - `adapters/jokeapi.ts` - REST клиент для JokeAPI
   - `content-cleaner.ts` - очистка и нормализация текста
   - `moderation.service.ts` - OpenAI moderation + стоп-слова
   - `duplicate-checker.ts` - проверка дубликатов по hash/ID
   - `category-filter.ts` - фильтрация категорий (white/black lists)
   - `proxy-manager.ts` - прокси/VPN конфигурация для geo restricted источников

4. **OpenAI**
   - `openai.service.ts` - обертка над OpenAI API
   - `text-processor.ts` - обработка текста
   - `metadata-generator.ts` - генерация метаданных

5. **Video Generation**
   - `video-generator.service.ts` - основной сервис
   - `background-generator.ts` - генерация фона
   - `text-renderer.ts` - рендеринг текста
   - `emoji-animator.ts` - анимация эмодзи
   - `audio-processor.ts` - обработка аудио
   - `music-selector.ts` - выбор музыки
   - `remotion/` - Remotion компоненты

6. **Music**
   - `music.service.ts` - управление музыкой
   - `music-generator.ts` - генерация через API (опционально)
   - `music-library.ts` - работа с библиотекой треков

7. **YouTube**
   - `youtube.service.ts` - работа с YouTube API
   - `oauth.handler.ts` - OAuth авторизация
   - `uploader.ts` - загрузка видео

8. **Queue System** (упрощенная для одного пользователя)
   - `queue.service.ts` - управление очередью через БД
   - `worker.ts` - обработка задач
   - **БЕЗ Redis** - используем MongoDB для статусов задач

9. **Monitoring & Logging**
   - `logger.ts` - winston/pino logger
   - `metrics.service.ts` - метрики по источникам (время ответа, ошибки)
   - `alert.service.ts` - уведомления при >5 ошибках подряд (email/telegram)

### API Routes

```
/api/auth/login          POST   - авторизация
/api/auth/logout         POST   - выход

/api/sources             GET    - список источников
/api/sources             POST   - добавить источник
/api/sources/[id]        DELETE - удалить источник
/api/sources/[id]/scrape POST   - создать ingest-задачу для источника

/api/scrape/jobs         GET    - список текущих задач (фильтр по статусу/источнику)
/api/scrape/jobs         POST   - создать задачу (bulk по нескольким категориям)
/api/scrape/jobs/[id]    GET    - статус задачи (progress, ошибки)
/api/scrape/jobs/[id]    DELETE - отмена/удаление задачи
/api/scrape/run          POST   - форсировать немедленный запуск worker (dev only)
/api/scrape/preview      POST   - получить превью 1 шутки (без сохранения) для проверки парсинга

/api/jokes               GET    - список анекдотов
/api/jokes/[id]          GET    - получить анекдот
/api/jokes/unused        GET    - неиспользованные
/api/jokes/[id]/flag     POST   - пометить как "needs review" или "blocked"

/api/videos              GET    - список видео
/api/videos/generate     POST   - сгенерировать видео
/api/videos/[id]         GET    - получить видео
/api/videos/[id]         DELETE - удалить видео
/api/videos/[id]/upload  POST   - загрузить на YouTube
/api/videos/jobs         GET    - посмотреть задачи генерации видео

/api/stats               GET    - статистика

/api/music               GET    - список треков
/api/music/upload        POST   - загрузить трек
/api/music/[id]          DELETE - удалить трек
/api/music/generate      POST   - сгенерировать трек (AI)

/api/debug/chistes       GET    - тестовый запрос к random API Chistes.com (dev)
/api/debug/yavendras     GET    - превью шуток из Yavendras (dev)
/api/debug/todochistes   GET    - превью шуток из TodoChistes (dev)
/api/debug/jokes-preview GET    - объединить все источники в единую выдачу (dev)

/api/ingest/preview      POST   - сохранить выбранные шутки в Mongo очередь
/api/ingest/queue        GET    - посмотреть очередь сохраненных шуток
/api/ingest/queue/reserve POST  - зарезервировать шутку для генерации (помечает status=reserved)
/api/ingest/queue/status POST  - обновить статус шутки (used/rejected/pending)
/api/ingest/run          POST   - запустить сбор шуток по дефолтной конфигурации

/api/youtube/auth        GET    - OAuth redirect
/api/youtube/callback    GET    - OAuth callback

/api/queue/status        GET    - статус очереди
/api/queue/[id]          GET    - статус задачи
```

## 📦 Зависимости (package.json)

### Основные
```json
{
  "dependencies": {
    "next": "^16.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.0.0",
    "tailwindcss": "^4.0.0",
    
    "mongodb": "^6.0.0",
    "mongoose": "^8.0.0",
    
    "openai": "^4.0.0",
    
    "googleapis": "^130.0.0",
    
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0",
    
    "cheerio": "^1.0.0",
    "puppeteer": "^22.0.0",
    
    "@remotion/bundler": "^4.0.0",
    "@remotion/cli": "^4.0.0",
    "@remotion/renderer": "^4.0.0",
    "remotion": "^4.0.0",
    
    "axios": "^1.6.0",
    "zod": "^3.22.0",
    
    "@remotion/lambda": "^4.0.0",
    
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/lib-storage": "^3.0.0",
    
    "suno-api": "^1.0.0",
    "socket.io": "^4.6.0",
    "socket.io-client": "^4.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^19.0.0",
    "@types/bcryptjs": "^2.4.0",
    "@types/jsonwebtoken": "^9.0.0",
    "@types/fluent-ffmpeg": "^2.1.0",
    "eslint": "^9.0.0",
    "eslint-config-next": "^16.0.0"
  }
}
```

## 🚀 План разработки (этапы)

### Фаза 1: Основа (Week 1)
- ✅ Инициализация Next.js проекта
- Настройка MongoDB подключения
- Создание моделей данных
- Система авторизации
- Базовый UI (Dashboard, Login)

### Фаза 2: Парсинг контента (Week 1-2)
- Модуль ingest + очередь задач (MongoDB)
- Адаптеры источников (Yavendras, TodoChistes, JokeAPI, Chistes.com)
- UI раздел "Источники" (просмотр, запуск, ошибки)
- Проверка дубликатов и хеширование текста
- Интеграция с OpenAI Moderation + стоп-слова
- Настройка white/black списков категорий
- Логирование/алерты при падении источников

### Фаза 3: Генерация видео (Week 2-3)
- Настройка Remotion
- Генерация фонов
- Рендеринг текста с анимацией
- Анимация эмодзи
- Интеграция музыки (библиотека треков)
- Обработка аудио с FFmpeg
- Композиция финального видео (9:16 формат)
- Настройка Remotion Lambda для рендеринга
- Интеграция с Cloudflare R2 для хранения
- UI для генерации и прогресса (polling статусов из БД)

### Фаза 4: YouTube интеграция (Week 3)
- OAuth2 настройка
- Загрузка видео
- Генерация метаданных

### Фаза 5: Тестирование и оптимизация (Week 4)
- Тестирование всего flow
- Оптимизация производительности
- Обработка ошибок
- Логирование
- UI/UX улучшения

## ⚠️ Потенциальные проблемы и решения

### 1. Длительность генерации видео на Vercel
**Проблема**: Vercel serverless functions имеют timeout (10 сек Hobby, 60 сек Pro)
**Решение** (несколько вариантов):

**Вариант A: Remotion Lambda (Рекомендуется)**
- Использовать Remotion Lambda для рендеринга в AWS
- Vercel API запускает рендеринг, Lambda делает видео
- Webhook возвращает результат в приложение
- Без ограничений по времени

**Вариант B: Vercel Cron Jobs + Long Running Tasks**
- Пользователь добавляет задачи в очередь (MongoDB)
- Vercel Cron каждые 5 минут проверяет очередь
- Запускает генерацию (через Remotion Lambda или внешний worker)

**Вариант C (для простоты MVP): Клиент-серверная генерация**
- Запуск процесса через API
- Polling статуса каждые 5 секунд
- Генерация происходит асинхронно

**Выбранный подход**: Вариант A с Remotion Lambda

### 2. Хранение видео и музыки на Vercel
**Проблема**: Vercel не подходит для хранения больших файлов
**Решение**:
- **Обязательно**: Cloudflare R2 (дешевле S3, без egress fees)
- Vercel Blob Storage для временных файлов
- Библиотека музыки: загрузить в R2/S3 (50-100 треков)
- Автоматическое удаление из R2 через 30 дней после загрузки на YouTube

### 3. Парсинг сайтов
**Проблема**: Сайты могут менять структуру
**Решение**:
- Гибкая система селекторов
- Уведомления о сбоях парсинга
- Возможность ручного добавления анекдотов

### 4. YouTube API лимиты
**Проблема**: Квоты на загрузку видео
**Решение**:
- Отслеживание использованной квоты
- Планирование загрузок
- Возможность ручной загрузки через скачивание

### 5. OpenAI API стоимость
**Проблема**: Частые запросы могут быть дорогими (10-20 видео/день)
**Решение**:
- Кеширование результатов обработки
- Использование GPT-4o-mini для простых задач
- Батчинг запросов
- Минимизация использования DALL-E (дорого)
- Альтернатива: Stability AI для генерации изображений (дешевле)

### 6. Генерация музыки
**Проблема**: AI генерация музыки может быть дорогой и медленной
**Решение** (поэтапный подход):
- **Этап 1 (MVP)**: YouTube Audio Library - бесплатная библиотека из 50-100 треков
- **Этап 2**: Добавить Epidemic Sound (подписка) или похожий сервис
- **Этап 3**: AI генерация (Suno/Mubert) для уникальных треков

### 7. Гео-блокировки источников
**Проблема**: Chistes.com блокирует трафик по GEO/IP (страницы не доступны из Европы)
**Решение**:
- Разместить ingest worker в допустимом регионе (например, Vercel Edge + AWS Lambda в США)
- Использовать прокси-провайдера (BrightData, ScraperAPI) с лимитом запросов
- Fallback на Yavendras/TodoChistes/JokeAPI при ошибках >3 подряд

### 8. Качество и модерация контента
**Проблема**: На сайтах есть взрослый/оскорбительный юмор → риск страйков YouTube
**Решение**:
- Жёсткий whitelist категорий (список хранится в настройках источника)
- Автоматическая модерация OpenAI + стоп-слова + проверка длины
- Ручная модерация спорных материалов (флаг `needs review`)
- Мониторинг жалоб/страйков, возможность быстро заблокировать шутку

## 🎨 Дизайн системы

### Цветовая схема
- Основной цвет: Синий (#3B82F6)
- Акцентный: Зеленый (#10B981) - для успешных действий
- Предупреждение: Желтый (#F59E0B)
- Ошибка: Красный (#EF4444)
- Фон: Светло-серый (#F9FAFB)
- Темный режим: Опционально

### Типографика
- Заголовки: Inter, Bold
- Текст: Inter, Regular
- Моноширинный: Fira Code (для кода/логов)

## 📊 Мониторинг и логирование

### Логирование
- Winston/Pino для структурированных логов
- Уровни: error, warn, info, debug
- Логи сохраняются в файлы и консоль

### Метрики для отслеживания
- Количество сгенерированных видео
- Среднее время генерации
- Количество успешных/неудачных загрузок на YouTube
- Использование OpenAI API
- Статус источников парсинга

## 🔒 Безопасность

1. **Аутентификация**: Хеширование паролей, JWT токены
2. **API Keys**: Все ключи в .env, никогда в коде
3. **Rate Limiting**: Ограничение запросов к API
4. **Валидация**: Zod для валидации всех входных данных
5. **CORS**: Настройка для безопасности
6. **Sanitization**: Очистка HTML при парсинге

## 📝 Дополнительные возможности (Future)

### Must-have для MVP
- Все основные функции из требований
- Система очередей для batch генерации
- Библиотека музыки (минимум 50 треков)
- Облачное хранилище видео

### Nice-to-have
- **Batch генерация**: кнопка "Сгенерировать 10 видео" с одним кликом
- **Планировщик**: автоматическая генерация N видео в день по расписанию
- A/B тестирование разных стилей видео
- Аналитика просмотров с YouTube
- Автоматическое определение лучшего времени для публикации
- История изменений (версионирование видео)
- Экспорт статистики
- Поддержка других языков (не только испанский)
- Голосовое озвучивание анекдотов (TTS)
- AI генерация уникальной музыки для каждого видео
- Автоматическая публикация на YouTube по расписанию

## 🎯 Метрики успеха

1. **Скорость генерации**: < 3 минуты на видео (одновременно до 5 видео)
2. **Производительность**: 20 видео за 1 час (при batch генерации)
3. **Качество**: 0% дубликатов анекдотов
4. **Надежность**: 95%+ успешных загрузок на YouTube
5. **UX**: Вся операция в 3 клика
6. **Автоматизация**: 90%+ процесса автоматизировано
7. **Музыка**: Уникальный трек для каждого видео (без повторов в течение недели)

---

## ✅ Уточненные детали

1. ✅ **Формат**: 9:16 (вертикальный для Shorts, 1080x1920)
2. ✅ **Музыка**: Да, фоновая музыка (разная, веселая/upbeat)
3. ✅ **Частота публикации**: 10-20 видео в день
4. ✅ **YouTube канал**: Будет создан
5. ✅ **Озвучка**: НЕ требуется (только текст)
6. ✅ **Источники**: Будут предоставлены позже (возможно с API)
7. ✅ **Платформа**: Vercel
8. ✅ **Redis**: НЕ используется (один пользователь)
9. ✅ **Примеры стиля**: 
   - [Пример 1](https://www.youtube.com/shorts/s1Z2i1J6BrE)
   - [Пример 2](https://www.youtube.com/shorts/hvlM_ksrW9U)

## ❓ Остались вопросы для уточнения (опционально)

1. **Фоны**: Только природа (лес, море, горы) или можно абстрактные/яркие градиенты?
   - Исходя из примеров - яркие, динамичные фоны подойдут

2. **Длительность**: Оптимальная длина видео? 
   - Рекомендация: 30-45 секунд (оптимально для Shorts)

3. **Шрифты**: Предпочтения по шрифтам? 
   - Рекомендация: Bold, без засечек, крупный размер (читаемость на мобильных)

---

## 📋 Чек-лист перед началом разработки

### Базовая инфраструктура
- [ ] Создать MongoDB Atlas аккаунт (бесплатный tier)
- [ ] ~~Redis~~ НЕ нужен (один пользователь)
- [ ] Получить OpenAI API ключ
- [ ] Настроить Cloudflare R2 (хранилище видео и музыки)
- [ ] Настроить Remotion Lambda в AWS (рендеринг видео)
- [ ] Создать проект на Vercel

### YouTube интеграция
- [ ] Создать YouTube канал
- [ ] Создать Google Cloud проект для YouTube API
- [ ] Настроить OAuth2 credentials для YouTube
- [ ] Получить первый токен авторизации

### Контент
- [ ] Получить список источников анекдотов
- [ ] Подготовить тестовые данные (10-20 анекдотов)
- [ ] Скачать библиотеку музыки (YouTube Audio Library - 50-100 треков)
- [ ] Определить стиль фонов и текста
- [ ] Составить whitelist/blacklist категорий для каждого сайта
- [ ] Подготовить список стоп-слов и фраз (NSFW, расизм, оскорбления)
- [ ] Настроить прокси/VPN для источников с geo-блокировкой (Chistes.com)

### Технические требования
- [x] ✅ Формат видео: 9:16 (1080x1920)
- [x] ✅ Целевой объем: 10-20 видео/день
- [x] ✅ Музыка: разная для каждого видео (веселая, разная)
- [x] ✅ Без озвучки (только текст)
- [x] ✅ Платформа: Vercel
- [x] ✅ Без Redis (один пользователь)
- [x] ✅ Примеры видео: предоставлены

---

**Документ готов к обсуждению и корректировкам перед началом разработки!**

