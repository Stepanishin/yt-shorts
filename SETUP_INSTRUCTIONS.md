# 📖 Инструкция по установке Shorts Generator

Подробная инструкция для второго пользователя по установке и настройке приложения.

---

## 📋 Содержание

- [Установка для macOS](#установка-для-macos)
- [Установка для Windows](#установка-для-windows)
- [Получение .env файла](#получение-env-файла)
- [Запуск приложения](#запуск-приложения)
- [Проверка работы](#проверка-работы)
- [Решение проблем](#решение-проблем)
- [Бонус: Удалённый доступ через Tailscale](#бонус-удалённый-доступ-через-tailscale)

---

## 🍎 Установка для macOS

### Шаг 1: Установка Homebrew (если не установлен)

Откройте **Terminal** (Finder → Программы → Утилиты → Terminal) и выполните:

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

Следуйте инструкциям на экране. После установки перезапустите Terminal.

### Шаг 2: Установка Node.js

```bash
brew install node
```

Проверьте установку:
```bash
node --version
npm --version
```

Должны увидеть версии (например: `v20.x.x` и `10.x.x`)

### Шаг 3: Установка FFmpeg

```bash
brew install ffmpeg
```

Проверьте установку:
```bash
ffmpeg -version
```

Должны увидеть информацию о версии FFmpeg.

### Шаг 4: Установка Git (если не установлен)

```bash
brew install git
```

Проверьте:
```bash
git --version
```

### Шаг 5: Клонирование репозитория

Перейдите в папку, где хотите разместить проект:
```bash
cd ~/Desktop
```

Клонируйте репозиторий (замените URL на реальный):
```bash
git clone <URL-репозитория>
cd shorts-generator
```

### Шаг 6: Установка зависимостей проекта

```bash
npm install
```

Это займёт 1-3 минуты. Дождитесь завершения.

### Шаг 7: Настройка .env файла

См. раздел [Получение .env файла](#получение-env-файла)

### Шаг 8: Запуск приложения

```bash
npm run dev
```

Откройте браузер: http://localhost:3000

✅ **Готово!** Приложение работает.

---

## 🪟 Установка для Windows

### Шаг 1: Установка Node.js

1. Перейдите на https://nodejs.org/
2. Скачайте **LTS версию** (рекомендуемая)
3. Запустите установщик
4. Следуйте инструкциям (оставьте всё по умолчанию)
5. Перезагрузите компьютер

Проверьте установку (откройте **Command Prompt** или **PowerShell**):
```cmd
node --version
npm --version
```

### Шаг 2: Установка Git

1. Перейдите на https://git-scm.com/download/win
2. Скачайте установщик
3. Запустите установку (оставьте всё по умолчанию)
4. Перезапустите Command Prompt

Проверьте:
```cmd
git --version
```

### Шаг 3: Установка FFmpeg

**Вариант А: Через Chocolatey (рекомендуется)**

1. Установите Chocolatey (откройте PowerShell **от имени администратора**):
```powershell
Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
```

2. Установите FFmpeg:
```powershell
choco install ffmpeg
```

3. Перезапустите PowerShell

**Вариант Б: Ручная установка**

1. Перейдите на https://www.gyan.dev/ffmpeg/builds/
2. Скачайте `ffmpeg-release-essentials.zip`
3. Распакуйте архив в `C:\ffmpeg`
4. Добавьте в PATH:
   - Нажмите `Win + R`
   - Введите: `sysdm.cpl`
   - Перейдите: Дополнительно → Переменные среды
   - В "Системные переменные" найдите `Path` → Изменить
   - Добавьте: `C:\ffmpeg\bin`
   - Нажмите OK везде
5. Перезагрузите компьютер

Проверьте:
```cmd
ffmpeg -version
```

### Шаг 4: Клонирование репозитория

Откройте **Command Prompt** или **PowerShell**:

```cmd
cd Desktop
git clone <URL-репозитория>
cd shorts-generator
```

### Шаг 5: Установка зависимостей проекта

```cmd
npm install
```

Дождитесь завершения (1-3 минуты).

### Шаг 6: Настройка .env файла

См. раздел [Получение .env файла](#получение-env-файла)

### Шаг 7: Запуск приложения

```cmd
npm run dev
```

Откройте браузер: http://localhost:3000

✅ **Готово!** Приложение работает.

---

## 🔑 Получение .env файла

### Вариант 1: Получить от первого пользователя

1. Попросите первого пользователя отправить вам файл `.env`
2. Скопируйте его в корневую папку проекта `shorts-generator`
3. **ВАЖНО:** Файл должен называться именно `.env` (с точкой в начале)

**На macOS/Linux:**
```bash
# Файл должен быть здесь:
ls -la ~/.../shorts-generator/.env
```

**На Windows:**
```cmd
# Файл должен быть здесь:
dir C:\Users\YourName\Desktop\shorts-generator\.env
```

### Вариант 2: Создать .env вручную

Если не можете получить файл, создайте его:

**На macOS:**
```bash
touch .env
nano .env
```

**На Windows:**
```cmd
notepad .env
```

Вставьте следующее содержимое (замените значения на реальные):

```env
# MongoDB
MONGODB_URI=mongodb+srv://username:password@cluster.xxxxx.mongodb.net/shorts-generator

# OpenAI (для генерации названий YouTube)
OPENAI_API_KEY=sk-...

# YouTube OAuth
YOUTUBE_CLIENT_ID=...
YOUTUBE_CLIENT_SECRET=...
YOUTUBE_REDIRECT_URI=http://localhost:3000/api/youtube/callback
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Pexels (для фоновых видео)
PEXELS_API_KEY=...
```

Сохраните файл.

---

## 🚀 Запуск приложения

### На macOS/Linux:

```bash
cd ~/Desktop/shorts-generator
npm run dev
```

### На Windows:

```cmd
cd Desktop\shorts-generator
npm run dev
```

### Что вы должны увидеть:

```
> shorts-generator@0.1.0 dev
> next dev

   ▲ Next.js 14.x.x
   - Local:        http://localhost:3000
   - Network:      http://192.168.x.x:3000

 ✓ Ready in 2.5s
```

Откройте браузер и перейдите: **http://localhost:3000**

---

## ✅ Проверка работы

1. **Главная страница**: Должен открыться интерфейс со списком анекдотов
2. **MongoDB**: Если видите список анекдотов — база данных работает
3. **Генерация видео**:
   - Откройте любой анекдот
   - Нажмите "Generate Background Video"
   - Подождите ~10-30 секунд
   - Должно появиться превью видео
4. **YouTube**:
   - Нажмите "Authorize YouTube"
   - Войдите в Google аккаунт
   - После этого можно публиковать видео

---

## 🔧 Решение проблем

### Проблема: "command not found: node"

**Решение (macOS):**
```bash
brew install node
```

**Решение (Windows):**
Переустановите Node.js с официального сайта и перезагрузите компьютер.

---

### Проблема: "command not found: ffmpeg"

**Решение (macOS):**
```bash
brew install ffmpeg
```

**Решение (Windows):**
Проверьте что FFmpeg добавлен в PATH. Перезагрузите компьютер.

---

### Проблема: "Cannot find module" или ошибки при npm install

**Решение:**
```bash
# Удалите node_modules и package-lock.json
rm -rf node_modules package-lock.json  # macOS/Linux
# или
rmdir /s node_modules & del package-lock.json  # Windows

# Переустановите зависимости
npm install
```

---

### Проблема: "Error connecting to MongoDB"

**Решение:**
1. Проверьте что `.env` файл существует и содержит правильный `MONGODB_URI`
2. Проверьте интернет-соединение
3. Попросите первого пользователя проверить настройки MongoDB Atlas (IP whitelist должен включать `0.0.0.0/0`)

---

### Проблема: Порт 3000 занят

**Решение (macOS/Linux):**
```bash
# Найдите процесс
lsof -i :3000

# Убейте процесс
kill -9 <PID>

# Или используйте другой порт
npm run dev -- -p 3001
```

**Решение (Windows):**
```cmd
# Найдите процесс
netstat -ano | findstr :3000

# Убейте процесс
taskkill /PID <PID> /F

# Или используйте другой порт
npm run dev -- -p 3001
```

---

### Проблема: ".env file not found"

**Решение:**
Файл `.env` должен быть в **корневой папке** проекта (там же где `package.json`).

**Проверьте (macOS/Linux):**
```bash
ls -la | grep .env
```

**Проверьте (Windows):**
```cmd
dir /a
```

Если файла нет — создайте его (см. раздел [Получение .env файла](#получение-env-файла))

---

## 🌐 Бонус: Удалённый доступ через Tailscale

Если хотите, чтобы **один человек запускал приложение**, а второй **подключался удалённо** — используйте Tailscale.

### Что это даёт:
- ✅ Не нужно устанавливать всё на втором компьютере
- ✅ Работает из любого места (дом, работа, кафе)
- ✅ Безопасное соединение (VPN)
- ✅ Бесплатно навсегда (до 100 устройств)

### Установка Tailscale

#### Для обоих пользователей:

**macOS:**
1. Перейдите на https://tailscale.com/download/mac
2. Скачайте и установите Tailscale
3. Запустите приложение
4. Войдите через Google/Microsoft/GitHub (используйте **один аккаунт** для обоих!)

**Windows:**
1. Перейдите на https://tailscale.com/download/windows
2. Скачайте и установите Tailscale
3. Запустите приложение
4. Войдите через Google/Microsoft/GitHub (используйте **один аккаунт** для обоих!)

### Как использовать:

#### Первый пользователь (у кого установлен проект):

1. Запустите приложение:
```bash
npm run dev
```

2. Откройте Tailscale и узнайте свой IP:
   - **macOS:** Кликните на иконку Tailscale в меню → скопируйте IP (например: `100.64.1.2`)
   - **Windows:** Кликните на иконку Tailscale в трее → скопируйте IP

3. Отправьте этот IP второму пользователю

#### Второй пользователь:

1. Убедитесь что Tailscale запущен и вы вошли в **тот же аккаунт**
2. Откройте браузер и перейдите на:
   ```
   http://<IP-первого-пользователя>:3000
   ```
   Например: `http://100.64.1.2:3000`

3. Всё работает как локально! 🎉

### Примечания:
- Tailscale IP не меняется, можно сохранить в закладки
- Оба должны быть подключены к Tailscale одновременно
- Первый пользователь должен держать приложение запущенным

---

## 📞 Нужна помощь?

Если что-то не работает:

1. Проверьте раздел [Решение проблем](#решение-проблем)
2. Убедитесь что все программы установлены (Node.js, FFmpeg, Git)
3. Проверьте что `.env` файл существует и содержит правильные данные
4. Попробуйте перезагрузить компьютер
5. Свяжитесь с первым пользователем

---

## ✨ Готово!

Теперь вы можете:
- 📝 Просматривать анекдоты
- 🎬 Генерировать видео
- 📤 Публиковать на YouTube
- 🎨 Редактировать контент

Удачи! 🚀
